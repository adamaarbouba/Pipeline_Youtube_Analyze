# `.gitignore`

Keeps secrets and generated runtime data out of Git.

Ignored:

```text
.env
Python caches
Airflow logs
raw JSON output
processed JSON output
```

`.gitkeep` files preserve empty runtime directories in the repository.
