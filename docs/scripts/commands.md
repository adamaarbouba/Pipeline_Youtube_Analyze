# `scripts/commands.txt`

A copy/paste command sheet for the project.

It covers:

```text
initial setup
Docker health
individual pipeline phases
manual end-to-end execution
Airflow triggering
PostgreSQL access
logs
shutdown/reset
```

Use:

```bash
cat scripts/commands.txt
```

or:

```bash
bat scripts/commands.txt
```
